# دليل النظام الجديد - نظام اللغتين

## ما الذي تم تغييره؟

تم إلغاء نظام الترجمة القديم (Django i18n) وإنشاء نظام جديد أبسط وأفضل يدعم اللغتين العربية والإنجليزية.

### ✅ المميزات الجديدة:

1. **نفس قاعدة البيانات**: الموقعان (العربي والإنجليزي) يستخدمان نفس قاعدة البيانات
2. **نفس لوحة التحكم**: إدارة واحدة للموقعين
3. **تبديل سهل**: زر في الصفحة الرئيسية للتبديل بين اللغتين
4. **لا مشاكل ترجمة**: كل المحتوى محفوظ في ملف واحد منظم
5. **أداء أسرع**: لا يوجد معالجة ترجمة إضافية

---

## كيف تستخدم النظام الجديد؟

### للمستخدمين:

1. افتح الموقع
2. ابحث عن أيقونة الكرة الأرضية (🌐) في أعلى الصفحة
3. اختر:
   - **العربية** للموقع بالعربي
   - **English** للموقع بالإنجليزي
4. الموقع سيتحول فوراً للغة المختارة

### للمطورين:

#### إضافة نص جديد:

1. افتح ملف `tourism/content.py`
2. ابحث عن القسم `'ar'` وأضف النص بالعربية:
   ```python
   'ar': {
       'اسم_المفتاح': 'النص بالعربية',
   }
   ```
3. ابحث عن القسم `'en'` وأضف النفس النص بالإنجليزية:
   ```python
   'en': {
       'اسم_المفتاح': 'Text in English',
   }
   ```
4. استخدمه في أي صفحة HTML:
   ```django
   {{ content.اسم_المفتاح }}
   ```

#### مثال عملي:

لنفترض تريد إضافة عنوان "خدماتنا":

1. في `content.py`:
   ```python
   CONTENT = {
       'ar': {
           # ... محتوى موجود
           'services_title': 'خدماتنا',
       },
       'en': {
           # ... existing content
           'services_title': 'Our Services',
       }
   }
   ```

2. في ملف HTML:
   ```html
   <h2>{{ content.services_title }}</h2>
   ```

---

## الملفات المهمة:

- **`tourism/content.py`**: يحتوي على جميع النصوص بالعربية والإنجليزية
- **`tourism/context_processors.py`**: يوفر المحتوى لجميع الصفحات
- **`tourism/views.py`**: يحتوي على وظيفة `set_language` للتبديل بين اللغات
- **`templates/tourism/base.html`**: فيه منتقي اللغة (Language Switcher)

---

## المحتوى من قاعدة البيانات:

### المشكلة:
أسماء الأماكن والأوصاف في قاعدة البيانات مازالت بلغة واحدة (عربي أو إنجليزي).

### الحل:
نضيف حقول جديدة للغة الثانية:

```python
# في models.py
class TouristPlace(models.Model):
    name = models.CharField(max_length=200)  # الاسم بالعربي
    name_en = models.CharField(max_length=200, blank=True)  # الاسم بالإنجليزي
    
    description = models.TextField()  # الوصف بالعربي
    description_en = models.TextField(blank=True)  # الوصف بالإنجليزي
```

ثم نشغل:
```bash
python manage.py makemigrations
python manage.py migrate
```

بعدها في لوحة التحكم نضيف الترجمة الإنجليزية لكل مكان.

---

## خطوات التشغيل:

### 1. تفعيل البيئة الافتراضية:
```powershell
.\venv\Scripts\Activate.ps1
```

### 2. تشغيل الخادم:
```bash
python manage.py runserver
```

### 3. افتح المتصفح:
```
http://127.0.0.1:8000/
```

---

## ما الذي يحتاج تحديث؟

✅ **تم تحديثه:**
- الصفحة الرئيسية (home.html)
- القالب الأساسي (base.html)
- القائمة الرئيسية
- منتقي اللغة

⏳ **يحتاج تحديث:**
- صفحة من نحن (about.html)
- صفحة اتصل بنا (contact.html)
- صفحات الأماكن (place_detail.html)
- صفحات المحافظات
- وغيرها...

### كيف تحدث باقي الصفحات؟

ببساطة:
1. افتح الملف (مثلاً `about.html`)
2. ابحث عن `{% trans "نص" %}`
3. استبدله بـ `{{ content.key }}`
4. إذا المفتاح (key) غير موجود في `content.py`، أضفه أولاً

---

## نصائح مهمة:

1. **استخدم أسماء واضحة للمفاتيح**:
   - ✅ جيد: `contact_title`, `about_description`
   - ❌ سيء: `text1`, `label2`

2. **نظّم المفاتيح حسب الصفحة**:
   ```python
   'ar': {
       # الصفحة الرئيسية
       'home_title': '...',
       'home_subtitle': '...',
       
       # صفحة من نحن
       'about_title': '...',
       'about_text': '...',
   }
   ```

3. **اجعل النصوص متطابقة بالمعنى**:
   - اللغتين يجب أن تعطيان نفس المعنى
   - لا تترجم حرفياً، ترجم المعنى

---

## أمثلة للاستخدام:

### 1. نص بسيط:
```html
<h1>{{ content.page_title }}</h1>
```

### 2. نص مع أيقونة:
```html
<button>
    <i class="fas fa-save"></i>
    {{ content.button_save }}
</button>
```

### 3. شرط حسب اللغة:
```html
{% if LANGUAGE_CODE == 'ar' %}
    <p>محتوى يظهر بالعربي فقط</p>
{% else %}
    <p>Content shown in English only</p>
{% endif %}
```

### 4. اتجاه السهم حسب اللغة:
```html
<i class="fas fa-arrow-{% if LANGUAGE_CODE == 'ar' %}left{% else %}right{% endif %}"></i>
```

---

## الأسئلة الشائعة:

### س: كيف أغير اللغة الافتراضية؟
ج: في ملف `tourism/context_processors.py`، غيّر هذا السطر:
```python
current_lang = request.session.get('language', 'ar')  # غيّر 'ar' إلى 'en'
```

### س: كيف أضيف لغة ثالثة؟
ج: 
1. أضف اللغة في `settings.py`
2. أضف قسم جديد في `CONTENT` في `content.py`
3. حدّث منتقي اللغة في `base.html`

### س: هل يمكن حذف اللغة العربية والإبقاء على الإنجليزية فقط؟
ج: نعم، لكن يفضل الإبقاء على كلاهما. إذا أردت:
1. غيّر اللغة الافتراضية إلى `'en'`
2. أخفِ خيار العربية من منتقي اللغة

### س: المحتوى لا يتغير عند تبديل اللغة؟
ج: 
1. تأكد من تحديث الصفحة (F5)
2. امسح الكوكيز
3. تحقق من أن المفتاح موجود في كلا اللغتين في `content.py`

---

## للمساعدة:

- راجع الدليل الشامل: `BILINGUAL_GUIDE.md`
- شاهد الأمثلة في `home.html` و `base.html`
- افتح `tourism/content.py` لترى جميع المفاتيح المتاحة

---

**تاريخ الإنشاء:** يناير 2026  
**الحالة:** نظام جديد يعمل - يحتاج استكمال باقي الصفحات

---

## ملخص سريع:

- ✅ تم إزالة نظام الترجمة القديم
- ✅ تم إنشاء نظام جديد أبسط
- ✅ الموقع يدعم العربية والإنجليزية
- ✅ نفس قاعدة البيانات ولوحة التحكم
- ✅ تبديل سهل بين اللغات
- ⏳ يحتاج تحديث باقي الصفحات
- ⏳ يحتاج إضافة ترجمة لمحتوى قاعدة البيانات

**النتيجة:** النظام يعمل بشكل ممتاز، فقط يحتاج استكمال! 🎉
